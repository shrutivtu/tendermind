// Award Sync Worker
// Pulls contract award notices (CANs) from TED SPARQL endpoint
// Extracts winner, price, buyer, CPV codes → upserts to awards table.
// Runs: nightly

console.log('Award Sync Worker — coming up next')
